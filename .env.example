# BITFUSE Environment Configuration Template
# Copy this file to .env.local and update with your actual values
# DO NOT commit .env.local to version control

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these values from your Supabase project dashboard
# URL format: https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous key for client-side operations (safe to expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Service role key for server-side operations (keep secret)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# =============================================================================
# APP CONFIGURATION
# =============================================================================
# Base URL for your application
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# WhatsApp support phone number (include country code)
# Format: +5511999999999 (Brazil format)
NEXT_PUBLIC_WHATSAPP_SUPPORT_PHONE=+5511999999999

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Uncomment and configure these if needed for your deployment

# Node environment (usually set automatically)
# NODE_ENV=development

# Next.js configuration
# NEXT_PUBLIC_VERCEL_URL=your-domain.vercel.app

# Additional app settings
# NEXT_PUBLIC_APP_NAME=BITFUSE
# NEXT_PUBLIC_SUPPORT_EMAIL=support@bitfuse.com

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Update the values above with your actual configuration
# 3. Never commit .env.local to version control
# 4. Keep your service role key secure - it has admin access to your database
# 5. The anon key is safe to expose as it's restricted by Row Level Security
# 6. Update NEXT_PUBLIC_APP_URL when deploying to production
# 7. Ensure your Supabase project has the correct CORS settings for your domain